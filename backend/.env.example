# DRMP后端环境变量配置示例
# 复制此文件为 .env 并填入实际值
# 注意：.env 文件不应提交到Git

# ==================== 数据库配置 ====================
DB_HOST=localhost
DB_PORT=3306
DB_NAME=drmp
DB_USERNAME=root
DB_PASSWORD=your_secure_password_here

# ==================== Redis配置 ====================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DATABASE=0

# ==================== JWT配置 ====================
# 使用以下命令生成强随机密钥：
# openssl rand -hex 32
JWT_SECRET=change_this_to_a_strong_random_string_at_least_32_chars_long
JWT_EXPIRATION=7200000
JWT_REFRESH_EXPIRATION=604800000

# ==================== 加密配置 ====================
# 用于敏感数据加密（身份证、手机号等）
# 使用以下命令生成：openssl rand -base64 32
ENCRYPTION_SECRET_KEY=change_this_to_another_strong_random_string_at_least_32_chars

# ==================== 文件上传配置 ====================
FILE_UPLOAD_DIR=./uploads
FILE_MAX_SIZE=104857600
FILE_TEMP_DIR=./temp
FILE_STORAGE_DIR=./storage

# ==================== CORS配置 ====================
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# ==================== 服务器配置 ====================
SERVER_PORT=8080

# ==================== 应用配置 ====================
SPRING_PROFILES_ACTIVE=dev

# ==================== 日志配置 ====================
LOG_LEVEL=INFO
LOG_FILE_PATH=./logs/drmp.log
